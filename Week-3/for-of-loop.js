// for (let i = 1; i <= 5; i++) {
//   // i +=2 if you want to increase 2
//   // to reassign the varaiable, we used the let NOT const
//   console.log("Count:" + i);
// }

const numbers = [1, 2, 3, 4, 5];

// color of colors
for (const num of numbers) {
  console.log(num);
}

const colo = [1, 2, 3, 4, 5];

for (const num of numbers) {
  console.log(num);
}
